---
title: GPUI Components
description: macOS controls built with GPUI and Rust, rendered live via WebAssembly.
---

Every component rendered live. Pick one to see variants, sizes, and the full API.

<ComponentsList />
